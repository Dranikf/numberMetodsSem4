% поиск y по данныому массиву х и коэффициентам полинома с те
% y(i) = (x(i)^0)* c(1) + (x(i)^1)* c(2)+ ... + (x(i)^(n-1))* c(n), n-1
% степень полинома
% последнюю версию и связь с разботчиком можно получить на GitHub.com/Dranikf
function y = KobFedGetPoliResult(c , x)
    n = numel(c);
    
    for i = 1:n
        
        y = y + c(i)*(x.^(i-1));
        
    end
    
end